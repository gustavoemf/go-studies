package main

import "fmt"

func main() {
	// 	Escreva expressões utilizando os seguintes operadores, e atribua seus valores a variáveis.
	// ==
	// !=
	// <=
	// <
	// =

	// Demonstre estes valores.
	comparacao := (1 == 0)
	diferenca := (1 != 0)
	menorigual := (1 <= 0)
	menor := (1 < 0)

	fmt.Printf("%v, %v , %v, %v", comparacao, diferenca, menorigual, menor)
}
